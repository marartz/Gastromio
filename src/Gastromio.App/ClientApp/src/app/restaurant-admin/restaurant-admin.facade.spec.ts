import { RestaurantAdmin.Facade.Ts } from './restaurant-admin.facade.ts';

describe('RestaurantAdmin.Facade.Ts', () => {
  it('should create an instance', () => {
    expect(new RestaurantAdmin.Facade.Ts()).toBeTruthy();
  });
});
